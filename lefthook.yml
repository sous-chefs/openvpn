pre-commit:
  parallel: true
  commands:
    cookstyle:
      glob: "*.rb"
      run: chef exec cookstyle --format simple {staged_files}
    rspec:
      glob: "*.rb"
      run: chef exec rspec --format progress
