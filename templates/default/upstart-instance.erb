description     "OpenVPN service instance"
 
stop on runlevel [016]
 
respawn
 
instance $NAME

env CONF_DIR=<%=@conf_dir%>

pre-start script
	test -f $CONF_DIR/${NAME}/conf || { stop ; exit 0; }
end script
	
 
exec openvpn --config <%=@conf_dir%>/${NAME}/conf
